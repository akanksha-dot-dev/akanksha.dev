---
id: WI-102
title: "Add Copy-to-Clipboard and Rich Node Tooltips in AI Sandbox"
batch: 1
status: queued
detail_tier: directed
files_targeted:
  - "src/components/AiSandbox.astro"
depends_on:
  - "WI-101"
acceptance_criteria:
  - "Add a 'Copy Output' button to the RAG synthesis card that copies the generated text with temporary 'Copied!' feedback"
  - "Add a 'Copy Logs' button to the Agent terminal that copies terminal text with feedback"
  - "Add explanatory tooltip / badge information on 'Semantic Router Node' and 'RAGAS Guardrails Node' toggles describing why they matter in production"
  - "Build passes cleanly: export PATH=\"/home/ag-deb/.nvm/versions/node/v22.22.3/bin:$PATH\" && pnpm run build"
created_at: "2026-08-21T11:32:00Z"
completed_at: null
---

## Intent

Recruiters and engineering directors want to share output snippets and understand why enterprise AI architecture features (like Semantic Routing and RAGAS Guardrails) are crucial. Adding clear copy buttons and concise architectural tooltips reinforces the "Production AI Engineer" narrative.

## Key Files

- `src/components/AiSandbox.astro`

## Specification

1. In the RAG synthesis card header, add a subtle copy button (`id="btn-copy-rag"`) with an SVG clipboard icon. When clicked, copy the plain text of `#rag-output-text` to `navigator.clipboard`, briefly changing text to "✓ Copied!".
2. In the Agent terminal header next to `akanksha@agent-node:~`, add a copy button (`id="btn-copy-agent"`) with similar clipboard behavior.
3. For the toggle nodes ("Semantic Router Node" and "RAGAS Guardrails Node"), add a small info helper icon / subtext (e.g. `title="Bypassing drops retrieval precision from 92% to 31%"` or inline micro-description `Prevents hallucination & noise`) that explains what happens when switched off.
4. Ensure all clipboard operations fail gracefully if `navigator.clipboard` is not available (using a fallback or try-catch).

## Constraints

- Only edit `src/components/AiSandbox.astro`.
- Keep the dark cyber-physical laboratory aesthetic consistent with `DESIGN_SYSTEM.md`.

## Verification

```bash
export PATH="/home/ag-deb/.nvm/versions/node/v22.22.3/bin:$PATH" && pnpm run build
```
