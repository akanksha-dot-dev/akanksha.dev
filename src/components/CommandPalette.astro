---
// CommandPalette.astro - Global Cmd+K Search & Quick Action Modal
---

<div id="cmd-k-modal" class="fixed inset-0 z-[100] hidden items-center justify-center p-4 sm:p-6 md:p-20 overflow-y-auto">
  <!-- Backdrop -->
  <div id="cmd-k-backdrop" class="fixed inset-0 bg-slate-950/80 backdrop-blur-md transition-opacity"></div>

  <!-- Modal Box -->
  <div class="relative w-full max-w-2xl bg-[#0a0d1a]/95 border border-white/15 rounded-2xl shadow-2xl shadow-cyan-950/50 overflow-hidden flex flex-col z-10 transition-all scale-100 opacity-100">
    
    <!-- Search Bar Input Header -->
    <div class="flex items-center px-4 py-3.5 border-b border-white/10 bg-white/[0.02]">
      <svg class="w-5 h-5 text-cyan-400 shrink-0 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/>
      </svg>
      <input 
        id="cmd-k-input" 
        type="text" 
        placeholder="Type a command or search portfolio (e.g. Agents, RAG, Samsung SDS)..." 
        class="w-full bg-transparent text-slate-100 placeholder-slate-400 text-sm md:text-base focus:outline-none font-(--font-body)"
        autocomplete="off"
        spellcheck="false"
      />
      <button id="cmd-k-close" class="px-2 py-1 text-xs text-slate-400 hover:text-white bg-white/5 rounded border border-white/10 transition-colors shrink-0 ml-2" aria-label="Close command palette">
        ESC
      </button>
    </div>

    <!-- Command Results Container -->
    <div id="cmd-k-results" class="max-h-[60vh] overflow-y-auto p-2 divide-y divide-white/5">
      <!-- Section: Navigation -->
      <div class="py-2" data-section="Navigation">
        <div class="px-3 py-1 text-[11px] font-mono font-semibold uppercase tracking-wider text-slate-400">Pages & Navigation</div>
        <div class="cmd-k-group flex flex-col gap-1 mt-1">
          <a href="/" class="cmd-k-item flex items-center justify-between px-3 py-2 rounded-xl hover:bg-white/10 transition-colors text-slate-200 group text-sm" data-search="home main start overview hero">
            <div class="flex items-center gap-3">
              <span class="p-1.5 rounded-lg bg-cyan-500/10 text-cyan-400 group-hover:bg-cyan-500/20">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"/></svg>
              </span>
              <span class="font-medium">Home Page</span>
            </div>
            <span class="text-xs text-slate-500 font-mono">/</span>
          </a>
          <a href="/about" class="cmd-k-item flex items-center justify-between px-3 py-2 rounded-xl hover:bg-white/10 transition-colors text-slate-200 group text-sm" data-search="about bio background profile skills education">
            <div class="flex items-center gap-3">
              <span class="p-1.5 rounded-lg bg-indigo-500/10 text-indigo-400 group-hover:bg-indigo-500/20">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"/></svg>
              </span>
              <span class="font-medium">About Akanksha</span>
            </div>
            <span class="text-xs text-slate-500 font-mono">/about</span>
          </a>
          <a href="/projects" class="cmd-k-item flex items-center justify-between px-3 py-2 rounded-xl hover:bg-white/10 transition-colors text-slate-200 group text-sm" data-search="projects flagship rag agentic vector python llm architecture work portfolio">
            <div class="flex items-center gap-3">
              <span class="p-1.5 rounded-lg bg-teal-500/10 text-teal-400 group-hover:bg-teal-500/20">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10"/></svg>
              </span>
              <span class="font-medium">Projects & Architecture</span>
            </div>
            <span class="text-xs text-slate-500 font-mono">/projects</span>
          </a>
          <a href="/experience" class="cmd-k-item flex items-center justify-between px-3 py-2 rounded-xl hover:bg-white/10 transition-colors text-slate-200 group text-sm" data-search="experience samsung sds hcltech work career history role senior ai engineer">
            <div class="flex items-center gap-3">
              <span class="p-1.5 rounded-lg bg-amber-500/10 text-amber-400 group-hover:bg-amber-500/20">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 13.255A23.931 23.931 0 0112 15c-3.183 0-6.22-.62-9-1.745M16 6V4a2 2 0 00-2-2h-4a2 2 0 00-2 2v2m4 6h.01M5 20h14a2 2 0 002-2V8a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/></svg>
              </span>
              <span class="font-medium">Work Experience</span>
            </div>
            <span class="text-xs text-slate-500 font-mono">/experience</span>
          </a>
          <a href="/speaking" class="cmd-k-item flex items-center justify-between px-3 py-2 rounded-xl hover:bg-white/10 transition-colors text-slate-200 group text-sm" data-search="speaking keynotes talks conferences devfest workshops tech talks presentation">
            <div class="flex items-center gap-3">
              <span class="p-1.5 rounded-lg bg-purple-500/10 text-purple-400 group-hover:bg-purple-500/20">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11a7 7 0 01-7 7m0 0a7 7 0 01-7-7m7 7v4m0 0H8m4 0h4m-4-8a3 3 0 100-6 3 3 0 000 6z"/></svg>
              </span>
              <span class="font-medium">Speaking & Workshops</span>
            </div>
            <span class="text-xs text-slate-500 font-mono">/speaking</span>
          </a>
          <a href="/blog" class="cmd-k-item flex items-center justify-between px-3 py-2 rounded-xl hover:bg-white/10 transition-colors text-slate-200 group text-sm" data-search="blog articles writing tech posts rag evaluation multi agent llm benchmarks">
            <div class="flex items-center gap-3">
              <span class="p-1.5 rounded-lg bg-rose-500/10 text-rose-400 group-hover:bg-rose-500/20">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 20H5a2 2 0 01-2-2V6a2 2 0 012-2h10a2 2 0 012 2v1m2 13a2 2 0 01-2-2V7m2 13a2 2 0 002-2V9a2 2 0 00-2-2h-2m-4-3H9M7 16h6M7 8h6v4H7V8z"/></svg>
              </span>
              <span class="font-medium">Blog & Technical Writing</span>
            </div>
            <span class="text-xs text-slate-500 font-mono">/blog</span>
          </a>
          <a href="/contact" class="cmd-k-item flex items-center justify-between px-3 py-2 rounded-xl hover:bg-white/10 transition-colors text-slate-200 group text-sm" data-search="contact email touch hire connect message">
            <div class="flex items-center gap-3">
              <span class="p-1.5 rounded-lg bg-emerald-500/10 text-emerald-400 group-hover:bg-emerald-500/20">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/></svg>
              </span>
              <span class="font-medium">Contact Akanksha</span>
            </div>
            <span class="text-xs text-slate-500 font-mono">/contact</span>
          </a>
        </div>
      </div>

      <!-- Section: Flagship AI Projects -->
      <div class="py-2" data-section="Projects">
        <div class="px-3 py-1 text-[11px] font-mono font-semibold uppercase tracking-wider text-slate-400">Featured Projects</div>
        <div class="cmd-k-group flex flex-col gap-1 mt-1">
          <a href="/projects#agentic-rag" class="cmd-k-item flex items-center justify-between px-3 py-2 rounded-xl hover:bg-white/10 transition-colors text-slate-200 group text-sm" data-search="agentic ai multi agent framework orchestration rag router tools">
            <div class="flex items-center gap-3">
              <span class="p-1.5 rounded-lg bg-cyan-500/10 text-cyan-400 font-mono text-xs font-bold">AI</span>
              <span class="font-medium">Agentic Multi-Agent Framework</span>
            </div>
            <span class="text-xs text-cyan-400/80 bg-cyan-950/60 px-2 py-0.5 rounded border border-cyan-500/20">Flagship</span>
          </a>
          <a href="/projects#multilingual-mandi" class="cmd-k-item flex items-center justify-between px-3 py-2 rounded-xl hover:bg-white/10 transition-colors text-slate-200 group text-sm" data-search="multilingual mandi voice rag Indic languages whisper agriculture voice ai">
            <div class="flex items-center gap-3">
              <span class="p-1.5 rounded-lg bg-teal-500/10 text-teal-400 font-mono text-xs font-bold">VO</span>
              <span class="font-medium">Multilingual Mandi Voice RAG</span>
            </div>
            <span class="text-xs text-teal-400/80 bg-teal-950/60 px-2 py-0.5 rounded border border-teal-500/20">Voice AI</span>
          </a>
          <a href="/projects#o9-optimizer" class="cmd-k-item flex items-center justify-between px-3 py-2 rounded-xl hover:bg-white/10 transition-colors text-slate-200 group text-sm" data-search="o9 optimizer agent supply chain enterprise autonomous llm tool use">
            <div class="flex items-center gap-3">
              <span class="p-1.5 rounded-lg bg-amber-500/10 text-amber-400 font-mono text-xs font-bold">OP</span>
              <span class="font-medium">o9 Optimizer Enterprise Agent</span>
            </div>
            <span class="text-xs text-amber-400/80 bg-amber-950/60 px-2 py-0.5 rounded border border-amber-500/20">Enterprise</span>
          </a>
        </div>
      </div>

      <!-- Section: Quick AI Assistant Actions -->
      <div class="py-2" data-section="AI Prompts">
        <div class="px-3 py-1 text-[11px] font-mono font-semibold uppercase tracking-wider text-slate-400">Quick AI Prompts</div>
        <div class="cmd-k-group flex flex-col gap-1 mt-1">
          <button type="button" class="cmd-k-item cmd-k-ai-prompt flex items-center justify-between px-3 py-2 rounded-xl hover:bg-cyan-500/15 transition-colors text-slate-200 group text-sm text-left w-full cursor-pointer" data-prompt="What is Akanksha's production AI tech stack?" data-search="ask ai tech stack skills tools python adk langgraph vector database">
            <div class="flex items-center gap-3">
              <span class="p-1.5 rounded-lg bg-cyan-400/10 text-cyan-300">✨</span>
              <span class="font-medium text-cyan-200">Ask AI: What is Akanksha's tech stack?</span>
            </div>
            <span class="text-xs text-cyan-400 font-mono">Prompt →</span>
          </button>
          <button type="button" class="cmd-k-item cmd-k-ai-prompt flex items-center justify-between px-3 py-2 rounded-xl hover:bg-cyan-500/15 transition-colors text-slate-200 group text-sm text-left w-full cursor-pointer" data-prompt="Explain Akanksha's AI achievements at Samsung SDS." data-search="ask ai samsung sds work achievements rag pipeline scale speed optimization">
            <div class="flex items-center gap-3">
              <span class="p-1.5 rounded-lg bg-cyan-400/10 text-cyan-300">✨</span>
              <span class="font-medium text-cyan-200">Ask AI: Samsung SDS AI achievements</span>
            </div>
            <span class="text-xs text-cyan-400 font-mono">Prompt →</span>
          </button>
        </div>
      </div>
    </div>

    <!-- Footer Controls Bar -->
    <div class="px-4 py-2.5 border-t border-white/10 bg-white/[0.02] flex items-center justify-between text-xs text-slate-400 font-mono">
      <div class="flex items-center gap-3">
        <span class="flex items-center gap-1"><kbd class="px-1 py-0.5 bg-white/10 rounded text-[10px]">↑</kbd><kbd class="px-1 py-0.5 bg-white/10 rounded text-[10px]">↓</kbd> Navigate</span>
        <span class="flex items-center gap-1"><kbd class="px-1 py-0.5 bg-white/10 rounded text-[10px]">↵</kbd> Select</span>
      </div>
      <span class="text-slate-500">akanksha.dev</span>
    </div>
  </div>
</div>

<script>
  function setupCommandPalette() {
    const modal = document.getElementById('cmd-k-modal');
    const backdrop = document.getElementById('cmd-k-backdrop');
    const closeBtn = document.getElementById('cmd-k-close');
    const input = document.getElementById('cmd-k-input') as HTMLInputElement | null;
    const items = Array.from(document.querySelectorAll<HTMLElement>('.cmd-k-item'));
    let activeIndex = -1;

    function openModal() {
      if (!modal) return;
      modal.classList.remove('hidden');
      modal.classList.add('flex');
      document.body.style.overflow = 'hidden';
      setTimeout(() => {
        input?.focus();
        input?.select();
      }, 50);
      resetActive();
    }

    function closeModal() {
      if (!modal) return;
      modal.classList.add('hidden');
      modal.classList.remove('flex');
      document.body.style.overflow = '';
      if (input) input.value = '';
      filterItems('');
    }

    function resetActive() {
      items.forEach(el => el.classList.remove('bg-white/15', 'border-cyan-500/40'));
      activeIndex = -1;
    }

    function updateActive(index: number) {
      const visibleItems = items.filter(el => !el.classList.contains('hidden'));
      if (visibleItems.length === 0) return;

      if (index < 0) index = visibleItems.length - 1;
      if (index >= visibleItems.length) index = 0;

      visibleItems.forEach(el => el.classList.remove('bg-white/15', 'border-cyan-500/40'));
      const target = visibleItems[index];
      if (target) {
        target.classList.add('bg-white/15');
        target.scrollIntoView({ block: 'nearest' });
      }
      activeIndex = index;
    }

    function filterItems(query: string) {
      const q = query.toLowerCase().trim();
      items.forEach(item => {
        const searchTerms = (item.dataset.search || '') + ' ' + (item.textContent || '');
        if (!q || searchTerms.toLowerCase().includes(q)) {
          item.classList.remove('hidden');
        } else {
          item.classList.add('hidden');
        }
      });
      resetActive();
    }

    // Event listeners
    document.addEventListener('keydown', (e) => {
      // Cmd+K or Ctrl+K
      if ((e.metaKey || e.ctrlKey) && e.key.toLowerCase() === 'k') {
        e.preventDefault();
        if (modal?.classList.contains('hidden')) {
          openModal();
        } else {
          closeModal();
        }
      } else if (e.key === 'Escape' && !modal?.classList.contains('hidden')) {
        closeModal();
      } else if (!modal?.classList.contains('hidden')) {
        const visibleItems = items.filter(el => !el.classList.contains('hidden'));
        if (e.key === 'ArrowDown') {
          e.preventDefault();
          updateActive(activeIndex + 1);
        } else if (e.key === 'ArrowUp') {
          e.preventDefault();
          updateActive(activeIndex - 1);
        } else if (e.key === 'Enter') {
          e.preventDefault();
          if (activeIndex >= 0 && activeIndex < visibleItems.length) {
            visibleItems[activeIndex].click();
          }
        }
      }
    });

    // Input filtering
    input?.addEventListener('input', (e) => {
      filterItems((e.target as HTMLInputElement).value);
    });

    // Triggers
    document.querySelectorAll('[data-cmd-k-trigger]').forEach(btn => {
      btn.addEventListener('click', (e) => {
        e.preventDefault();
        openModal();
      });
    });

    // Close button & backdrop
    closeBtn?.addEventListener('click', closeModal);
    backdrop?.addEventListener('click', closeModal);

    // Item click closing
    items.forEach(item => {
      item.addEventListener('click', (e) => {
        const prompt = item.dataset.prompt;
        if (prompt) {
          // If it's an AI prompt item, dispatch event for AI Assistant if open or redirect to contact/assistant
          const event = new CustomEvent('open-ai-prompt', { detail: { prompt } });
          window.dispatchEvent(event);
        }
        closeModal();
      });
    });
  }

  // Run on initial load and view transition swap
  setupCommandPalette();
  document.addEventListener('astro:page-load', setupCommandPalette);
</script>
